# MuseScore AI Bridge

This project connects **MuseScore 4** with local AI models using **Ollama** and **Qwen2.5-Coder**. It allows you to control your sheet music using natural language.

---

## 1. Prerequisites (Docker & Ollama)
Run these two commands in your terminal to download and start the local AI model:

```bash
docker run -d -v ollama:/root/.ollama -p 11434:11434 --name ollama ollama/ollama
# or with Nvidia GPU 
docker run -d --gpus=all -v ollama:/root/.ollama -p 11434:11434 --name ollama ollama/ollama


docker exec -it ollama ollama run qwen2.5-coder:3b
# if you use a bigger model you have to modify also ollama_bridge.py
# at line 76
```

---

## 2. Python Setup
Open your terminal inside the project folder and run these commands to set up the environment:

```bash
python3 -m venv venv
source venv/bin/activate
pip install httpx
```
*(On Windows, use `.\venv\Scripts\Activate.ps1` to activate the venv).*

---

## 3. MuseScore Plugin Setup

1. Copy the `.qml` file from the `plugin-musescore/` folder into your MuseScore Plugins directory:
   - **Linux:** `~/Documents/MuseScore4/Plugins/`
   - **macOS:** `/Users/YOUR_USERNAME/Documents/MuseScore4/Plugins/`
   - **Windows:** `C:\Users\YOUR_USERNAME\Documents\MuseScore4\Plugins\`

2. Open **MuseScore 4**, go to **Home -> Plugins**, and check the box to **Enable** the plugin.

3. **IMPORTANT:** Every time you open a music score, go to the top **Plugins** menu and click on the plugin's name to activate it.

---

## 4. How to Use

1. Make sure your Docker container is running.
2. Open your score in MuseScore and launch the plugin.
3. Start the Python bridge script:
   ```bash
   python3 ollama_bridge.py
   ```
4. Type your commands in the terminal (e.g., *"Adatta il rigo 0 per basso a 4 corde"*).

---

## License
This project is licensed under the MIT License.

## Credits
This project is a fork/adaptation based on the original work by [ghchen99/mcp-musescore](https://github.com/ghchen99/mcp-musescore).